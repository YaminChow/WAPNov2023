const getName = require('./pattern1');
getName.getName();

//when called the getName, that time we get the Object not the function. We need to add the function of this object that time we can get the correct result.